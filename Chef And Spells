#include <bits/stdc++.h>
using namespace std;
int main()
{
	long T;
	cin>>T;
	while(T--)
	{
		long arr[T],i;
		for(i=0;i<=2;i++)
		{
			cin>>arr[i];
		}
        sort(arr, arr + 3, greater<long>());
		long sum=arr[0]+arr[1];
		cout<<sum<<"\n";  
	}
	return 0;
}
